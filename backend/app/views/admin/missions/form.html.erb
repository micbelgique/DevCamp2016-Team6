<div class="container">
  <div class="row">
    <div class="panel col-md-6 col-md-offset-3">
      <h1><%= params[:action].in?(['new', 'create']) ? "Ajouter une mission" : "Modifier une mission" %></h1>

      <%= render 'shared/flash' %>

      <%= form_for([:admin, @mission], :html => { :class => 'form-horizontal' }) do |f| %>
        <div class="form-group">
          <%= f.label :name, 'Nom', :class => 'control-label col-md-3' %>

          <div class="col-md-9">
            <%= f.text_field :name, :class => 'form-control' %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :tagline, 'Accroche', :class => 'control-label col-md-3' %>

          <div class="col-md-9">
            <%= f.text_field :tagline, :class => 'form-control' %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :description, 'Description', :class => 'control-label col-md-3' %>

          <div class="col-md-9">
            <%= f.text_area :description, :class => 'form-control', :rows => 5 %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :picture, 'Photo', :class => 'control-label col-md-3' %>

          <div class="col-md-9">
            <%= f.file_field :picture, :class => 'form-control' %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :latitude, 'Latitude', :class => 'control-label col-md-3' %>

          <div class="col-md-9">
            <%= f.text_field :latitude, :class => 'form-control' %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :longitude, 'Longitude', :class => 'control-label col-md-3' %>

          <div class="col-md-9">
            <%= f.text_field :longitude, :class => 'form-control' %>
          </div>
        </div>

        <div class="actions">
          <%= link_to 'Annuler', admin_missions_path, :class => 'btn btn-sm btn-default' %>
          <%= f.submit 'Enregistrer', :class => 'btn btn-sm btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
